// Write a function that prints "condition was true" if a given number if less than 5 and prints "condition was false" if the given number is greater than 5.
use branches::check_condition;
use branches::using_if_in_let_statement;

fn main() {
    let number = 3;
  check_condition(number);
  using_if_in_let_statement();
}
